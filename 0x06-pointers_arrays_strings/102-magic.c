#include <stdio.h>

/**
 * main - write line of code here.
 * Remember:
 * 'a' is not allowed.
 * 'p' modification is not allowed.
 * use only one statement.
 * only this line of code is allowed.
 * no other code is allowed.
 *
 * Return: 0.
 */

int main(void)
{
	int n;
	int a[5];
	int *p;

	a[2] = 1024;
	p = &n;

	printf("a[2] = %d\n", a[2]);
	return (0);
}
